# improve-codebase-architecture

Vendored from [mattpocock/skills](https://github.com/mattpocock/skills/tree/main/skills/engineering/improve-codebase-architecture) (MIT).

Invoke in Cursor as **`/improve-codebase-architecture`**.

## Files

- `SKILL.md` — main skill workflow (explore → HTML report → grilling loop)
- `HTML-REPORT.md` — report scaffold, diagram patterns, and styling guidance

## Companion skills (optional)

This skill references other Matt Pocock engineering skills that are not vendored here yet:

- `/codebase-design` — architecture vocabulary (module, seam, depth, …)
- `/grilling` — design grilling loop after you pick a candidate
- `/domain-modeling` — keep `CONTEXT.md` and ADRs current

Install the full set with `npx skills@latest add mattpocock/skills`, or add individual skills under `skills/engineering/`.

## Project setup

Run `/setup-matt-pocock-skills` once to configure issue tracker, triage labels, and domain doc layout (`CONTEXT.md`, `docs/adr/`).
